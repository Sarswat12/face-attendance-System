# Copy to .env and edit values
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=Sarswati@18
DB_NAME=face_attendance
# Use a long, random secret in production. Example generation:
# python -c "import secrets; print(secrets.token_urlsafe(48))"
JWT_SECRET_KEY=change-me-to-a-secure-value
FLASK_ENV=development
API_HOST=0.0.0.0
API_PORT=8000
REFRESH_COOKIE_NAME=refresh_token
# Whether to use Secure flag on refresh cookie. Set to true in production.
REFRESH_COOKIE_SECURE=false
REFRESH_TOKEN_EXPIRES_DAYS=30
# NOTE: This application requires a MySQL database. Do NOT enable
# `USE_SQLITE` or `DEV_FALLBACK_SQLITE` — they are ignored and the app
# will refuse to start if MySQL is not configured or reachable.
